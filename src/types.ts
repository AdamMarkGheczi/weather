export interface City {
    name: string,
    region: string,
    id: number
}

export interface SearchInfo{
    id: number,
    name: string,
    region: string,
    country: string,
    lat: number,
    lon: number,
    url: string    
}